[mysqld]
user = root
skip-networking=FALSE
port = 3306
# bind_address = 127.0.0.1,::1,host.docker.internal
bind_address = *
datadir = /app/mysql
log-bin = /app/mysql-bin
socket=/run/mysqld/mysqld.sock
character-set-server = utf8mb4
# character-set-server = utf8
collation-server = utf8mb4_general_ci
# collation-server = utf8_unicode_ci
default-authentication-plugin=mysql_native_password

[client]
default-character-set = utf8mb4
# default-character-set = utf8

# refresh
# docker cp .\docker\alpine-mysql\conf\.my.cnf wp_qs_test_db:/etc/my.cnf.d/zzz-my.cnf
# docker-compose exec database  tail /etc/my.cnf.d/zzz-my.cnf
# restart docker container
